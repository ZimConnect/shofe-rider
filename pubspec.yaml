
name: shofe_rider
description: "Shofe Rider App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: "3.22.3"

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase - Versions that 100% work with Flutter 3.22.3
  firebase_core: ^2.25.7
  firebase_auth: ^4.17.9
  firebase_firestore: ^4.17.4  # <- KEY FIX
  firebase_storage: ^11.6
