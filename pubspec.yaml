name: shofe_rider
description: "Shofe Rider App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # === V2 PLUGINS - THESE FIX THE BUILD ERROR ===
  geolocator: ^11.0.0             
  google_maps_flutter: ^2.7.0      
  flutter_polyline_points: ^2.1.0  

  # === YOUR OTHER PACKAGES ===
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/
