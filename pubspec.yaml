dependencies:
  flutter:
    sdk: flutter

  # STATE MANAGEMENT
  provider: ^6.1.2

  # HTTP + JSON
  http: ^1.1.2

  # GOOGLE MAPS
  google_maps_flutter: ^2.5.0

  # FIREBASE - OLD STABLE V2 FOR FLUTTER 3.22.3
  firebase_core: ^2.8.0
  firebase_auth: ^4.6.3
  firebase_firestore: ^4.4.5

  # LOCATION + PERMISSIONS
  permission_handler: ^11.3.1
  geolocator: ^10.0.0
