
name: shofer_rider
description: "Shofe Rider Driver App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # THE 4 V1 CULPRITS - UPDATED TO V2 ONLY
  webview_flutter: ^4.7.0
  google_maps_flutter: ^2.7.0
  permission_handler: ^11.3.1
  geolocator: ^13.0.2

  # KEEP ALL YOUR OTHER PACKAGES HERE
  # Example:
  # http: ^1.2.2
  # provider: ^6.1.2
  # firebase_core: ^3.6.0
  # firebase_auth: ^5.3.1
  # shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
