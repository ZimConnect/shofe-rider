
name: shofe_rider
description: Shofe Rider App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  
  # V2 PLUGINS ONLY
  mobile_scanner: ^5.1.2
  webview_flutter: ^4.7.0
  geolocator: ^13.0.2
  google_maps_flutter: ^2.7.0
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
